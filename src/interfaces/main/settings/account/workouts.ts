export interface IWorkout {
  calories: number;
  durationMinutes: number;
  id: number;
  imageUrl: string;
  title: string;
  watchedDate: string;
}
export interface IFavWorkout {
  id: number;
  imageUrl: string;
  title: string;
}
